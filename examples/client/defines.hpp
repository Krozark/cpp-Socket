#ifndef DEFINES_HPP
#define DEFINES_HPP

#include <Socket/define.hpp>

/**
 * \brief store the erros codes
 */
enum ERRORS {
    STOP = NTW_STOP_CONNEXION,
    OK = 0,
    INPUT_NOT_VALID = 1,
    EMPTY_INPUT = 2,
};

#endif
